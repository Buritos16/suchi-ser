type DateValue = Date | number | string;
/**
 * Форматирует дату в понятную для QIWI строку:
 *
 * `ГГГГ-ММ-ДДTЧЧ:ММ:СС+\-ЧЧ:ММ`
 *
 * @param {DateValue} dateTime Аргумент для конструктора
 * @return {string}
 */
export declare function formatDate(dateTime: DateValue): string;
/**
 * **Используется только для параметра `lifetime` при создании ссылки на форму оплаты**
 *
 * Форматирует дату в понятную для QIWI строку:
 *
 * `ГГГГ-ММ-ДДTччмм`
 *
 * @export
 * @param {DateValue} dateTime
 * @return {string} string
 */
export declare function formatAltLifetimeDate(dateTime: DateValue): string;
export declare enum TimeSpan {
    Millisecond = 1,
    Second = 1000,
    Minute = 60000,
    Hour = 3600000,
    Day = 86400000,
    Week = 604800000,
    Month = 2592000000,
    Year = 31536000000
}
export declare const TimeSpanMapping: Readonly<{
    ms: TimeSpan;
    msec: TimeSpan;
    millisecond: TimeSpan;
    s: TimeSpan;
    sec: TimeSpan;
    second: TimeSpan;
    m: TimeSpan;
    min: TimeSpan;
    minute: TimeSpan;
    h: TimeSpan;
    hr: TimeSpan;
    hour: TimeSpan;
    d: TimeSpan;
    day: TimeSpan;
    w: TimeSpan;
    week: TimeSpan;
    mn: TimeSpan;
    mon: TimeSpan;
    month: TimeSpan;
    y: TimeSpan;
    yr: TimeSpan;
    year: TimeSpan;
}>;
export type TimeSpanMapping = typeof TimeSpanMapping;
export type TimeSpanKeys = keyof TimeSpanMapping;
type TimeUnit = TimeSpan | number | TimeSpanKeys;
/**
 * **Используется только для параметра `lifetime` при создании ссылки на форму оплаты**
 *
 * Форматирует дату в понятную для QIWI строку:
 *
 * `ГГГГ-ММ-ДДTччмм`
 *
 * @export
 * @param {number} amount
 * @param {(TimeSpan | number | TimeSpanKeys)} unit
 * @param {Date} [currentDate=new Date()]
 * @return {string} string
 */
export declare function formatOffsetDate(amount: number, unit?: TimeUnit, currentDate?: Date): string;
/**
 *
 *
 * @export
 * @param {number} amount
 * @param {(TimeSpan | number | TimeSpanKeys)} unit
 * @param {Date} [currentDate=new Date()]
 * @return {string} string
 */
export declare function formatOffsetAltLifetimeDate(amount: number, unit?: TimeUnit, currentDate?: Date): string;
export {};
