import { Application } from "express";
export type MockServer = {
    url: string;
    app: Application;
    close(): void;
};
/**
 *
 * @return {Promise<MockServer>}
 */
export declare function createMockServer(): Promise<MockServer>;
